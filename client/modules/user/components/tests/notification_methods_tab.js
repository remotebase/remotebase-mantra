const {describe, it} = global;
import {expect} from 'chai';
import {shallow} from 'enzyme';
import NotificationMethodsTab from '../notification_methods_tab';

describe('user.components.notification_methods_tab', () => {
  it('should do something');
});
