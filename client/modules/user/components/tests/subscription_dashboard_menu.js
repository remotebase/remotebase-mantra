const {describe, it} = global;
import {expect} from 'chai';
import {shallow} from 'enzyme';
import SubscriptionDashboardMenu from '../subscription_dashboard_menu';

describe('user.components.subscription_dashboard_menu', () => {
  it('should do something');
});
