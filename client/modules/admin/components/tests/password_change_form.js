const {describe, it} = global;
import {expect} from 'chai';
import {shallow} from 'enzyme';
import PasswordChangeForm from '../password_change_form';

describe('admin.components.password_change_form', () => {
  it('should do something');
});
