const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../companies';

describe('core.actions.companies', () => {
  it('should do something');
});
