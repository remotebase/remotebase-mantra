const {describe, it} = global;
import {expect} from 'chai';
import {shallow} from 'enzyme';
import CommunicationFilters from '../communication_filters';

describe('core.components.communication_filters', () => {
  it('should do something');
});
