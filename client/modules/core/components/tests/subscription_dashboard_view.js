const {describe, it} = global;
import {expect} from 'chai';
import {shallow} from 'enzyme';
import SubscriptionDashboardView from '../subscription_dashboard_view';

describe('core.components.subscription_dashboard_view', () => {
  it('should do something');
});
