const {describe, it} = global;
import {expect} from 'chai';
import {shallow} from 'enzyme';
import MockSubscribeButton from '../mock_subscribe_button';

describe('core.components.mock_subscribe_button', () => {
  it('should do something');
});
