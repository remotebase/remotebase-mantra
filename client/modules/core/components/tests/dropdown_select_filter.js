const {describe, it} = global;
import {expect} from 'chai';
import {shallow} from 'enzyme';
import DropdownSelectFilter from '../dropdown_select_filter';

describe('core.components.dropdown_select_filter', () => {
  it('should do something');
});
