const {describe, it} = global;
import {expect} from 'chai';
import {shallow} from 'enzyme';
import CompanyItemCardView from '../company_item_card_view';

describe('core.components.company_item_card_view', () => {
  it('should do something');
});
