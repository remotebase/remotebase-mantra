const {describe, it} = global;
import {expect} from 'chai';
import {shallow} from 'enzyme';
import SubscriptionDashboard from '../subscription_dashboard';

describe('core.components.subscription_dashboard', () => {
  it('should do something');
});
