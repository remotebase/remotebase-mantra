const {describe, it} = global;
import {expect} from 'chai';
import {shallow} from 'enzyme';
import CollaborationFilters from '../collaboration_filters';

describe('core.components.collaboration_filters', () => {
  it('should do something');
});
